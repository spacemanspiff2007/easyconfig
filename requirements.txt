# runtime dependencies
pydantic            >= 1.9.0,   < 2.0
ruamel.yaml         >= 0.17,    < 0.18
typing-extensions   >= 4.1,     < 5

# testing dependencies
pytest     >= 7.1,  < 8
pre-commit >= 2,    < 3
